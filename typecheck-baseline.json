{
  "_comment": "mypy ratchet baseline. Regenerate with `make typecheck-baseline` and commit. `total`/`packages` gate the repo-wide ratchet; `legacy_files` lists files that already had errors when the gate was introduced, which the changed-files gate reports but does not block on. Never let these numbers grow.",
  "mypy_version": "2.3.1",
  "mypy_args": [
    "--ignore-missing-imports"
  ],
  "roots": [
    "src",
    "tests",
    "bin"
  ],
  "total": 0,
  "packages": {},
  "legacy_files": []
}
