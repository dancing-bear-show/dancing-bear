name: branch-hygiene-sweep
version: "1.0"
description: >
  Safely identify and delete stale git branches and worktrees. Every stage is
  biased toward "prove it is safe before deleting" — the default is to spare a
  branch when uncertain. Nothing is removed until a human gate approves the
  plan, and nothing is removed at all when dry_run is true — that includes
  .venv directories, which reclaim-venvs also skips under dry_run. dry_run is
  a global kill switch on every destructive action in this workflow, with no
  exceptions; a preview run leaves the tree byte-identical.

  Key correctness rules encoded throughout this workflow:
    - Never use git cherry alone as a merged/unmerged signal — it cannot detect
      squash-merges and falsely flags already-merged branches as unmerged.
    - Never use git diff with two-dot or three-dot ranges as a merged-ness test.
    - The authoritative merged signal is the GitHub PR record + merge-base check.
    - Always fetch and prune before any comparison — stale local origin/main
      gives false "not merged" readings for recently merged PRs.
    - Rebase-merged PRs have a mergeCommit that is NOT an ancestor of main;
      fall back to content comparison, not rejection.
    - Check for descendant branches and live worktrees before proposing deletion.
    - Use reflog timestamps and worktree dir birth/mtime, not committerdate.
    - Never use grep -P (unavailable on macOS BSD grep); use awk or plain grep.
    - Orphaned worktree directories that git worktree list does not track must
      be removed as plain directories, not via git worktree remove.
    - A worktree's dirty status may reflect the shared checkout bleeding through
      when the worktree is sparse/broken; verify files physically exist there.
    - Uncommitted WIP in a worktree is real work that dies with the directory;
      always diff it against main and surface it separately from committed work.

  Stage dependency graph:

    gather-inventory
          |
    classify-buckets  (produces items for fan-out)
          |
    verify-candidates × N  (parallel fan-out per bucket, adversarial)
          |
    cross-check
          |
    report
          |
    human-gate  [human gate — nothing deleted until approved]
          |
    execute-deletions  (always runs; no-ops and writes a sentinel when dry_run=true)
          |
    reclaim-venvs  (honours dry_run; only touches KEPT worktrees)
          |
    verify-post

  DISK NOTE: measured on this repo, a worktree averages ~350M of which ~348M
  is .venv and only ~27M is source — a worktree is ~93% virtualenv. Deleting
  branches reclaims almost nothing (refs are bytes; .git is 57M) and does not
  speed git up (git status runs in ~40ms at 147 branches). The real disk lever
  is reclaim-venvs, which purges regenerable .venv directories from worktrees
  that are being KEPT. That is strictly lower-risk than deleting a worktree:
  a .venv is rebuilt with `make venv`, whereas a deleted worktree takes any
  uncommitted WIP with it. Treat deletion as the hygiene goal and venv
  reclamation as the space goal — they are separate levers.

  RUNNING THIS WORKFLOW: run it from a NON-ISOLATED session. That is the whole
  precondition, and it is on the caller — the stages below are ordinary agent
  stages and this YAML has no way to express or enforce it.

  Why: a spawned subagent INHERITS its session's worktree isolation, and an
  isolated agent is refused any git command aimed at the shared checkout —
  which is this workflow's entire input. A first real run failed immediately at
  gather-inventory with WORKTREE_ISOLATION_CONSTRAINT_VIOLATION. Sessions are
  normally launched with `claude -w`, so an isolated session is the default
  case, not an edge case.

  How to satisfy it:
    - Launch the session without `-w`, so spawned stages do not inherit
      isolation. This is the only option that works as this file stands, and
      it works under both runners.

  There is a second approach, but it is not available without changing this
  file and choosing the runner deliberately: mark the git-reading stages
  `executor: inline` so the orchestrator runs them itself instead of spawning
  agents. No stage here is marked inline today, and `executor: inline` is
  supported ONLY by the Claude /workflow skill — `./bin/workflow run` raises
  NotImplementedError on it ("the Python dispatcher does not implement inline
  execution"). Adopting it therefore means editing these stages AND running
  through the skill; under ./bin/workflow run it fails outright.

  For the record, on the first real run neither route was used. The spawned
  gather-inventory agent failed on isolation, and the orchestrator then carried
  out each stage's described steps directly, from an ordinary session — no
  `executor: inline` was added, and this file was not modified to make the run
  work. `./bin/workflow run` cannot reproduce that: it dispatches every stage
  to an agent, so it needs the non-isolated session named above.

  Stating it precisely because the distinction is easy to blur: "the
  orchestrator ran the stages" describes who executed the steps, NOT a
  supported executor mode. The `executor: inline` route above remains
  unexercised — no stage in this file has ever declared it.

  WHY ISOLATION BREAKS IT: an isolated worktree cannot see the parent
  checkout's branches, reflog, or worktree list, and every stage here needs a
  live view of the real git state. Note this is a property of the SESSION, not
  something the stages opt into: there is no "must not be isolated" field to
  set, and a subagent inherits whatever the session has. That is why the
  precondition above lands on the caller.

metadata:
  author: dancing-bear
  created: "2026-08-27"

trigger:
  source: manual
  params:
    idle_hours: "48"          # worktrees idle at least this long are candidates
    dry_run: "true"           # global kill switch: when true NOTHING is deleted,
                              # branches, worktrees and .venv alike. Report only.
    include_worktrees: "true" # also sweep orphaned worktree directories
    protect: ""               # comma-list of branch names to never touch (e.g. main,develop)
    reclaim_venvs: "true"     # purge .venv from KEPT worktrees idle > venv_idle_hours
    venv_idle_hours: "24"     # a .venv idle this long is regenerable dead weight

workspace_dir: "{work_dir}/branch-hygiene-sweep"

stages:

  # ===========================================================================
  # G0: Gather inventory — fetch, prune, enumerate branches, worktrees, PRs
  # ===========================================================================

  - name: gather-inventory
    kind: gather
    depends_on: []
    description: >
      Collect a full inventory of local branches, worktrees, reflog ages,
      filesystem timestamps, and GitHub PR state. Write raw data for
      classify-buckets to work from.

      GOTCHA 1 — git cherry cannot detect squash-merges.
        Do NOT run git cherry at any point in this workflow. It compares by
        patch-id; a squash merge rewrites N commits into one, so every original
        commit reads as "+" (unmerged). In real runs this falsely flagged 41 of
        49 already-merged branches as carrying unique work. This workflow never
        uses git cherry.

      GOTCHA 2 — git diff range operators are not merged-ness tests.
        Do NOT use git diff origin/main...branch (three-dot) or
        git diff origin/main..branch (two-dot) to decide if a branch is merged.
        Three-dot diffs from the merge base, reporting main's newer work as
        absent — this looks like divergence. Two-dot has a mirror-image problem.
        Neither answers "is this work already in main".

      Step 1 — fetch and prune (GOTCHA 3a: stale local origin/main causes
        false "not merged" for recently merged PRs).
          git fetch origin --prune
          git remote prune origin

      Step 2 — enumerate local branches.
          git branch --format="%(refname:short)" | sort
        For each branch, collect:
          - Last reflog timestamp (GOTCHA 5: committerdate is useless for
            ephemeral claude -w branches that inherit the base commit's date):
              git reflog show --date=unix <branch> | head -1
            Parse the unix timestamp from the output.
          - Whether it has a remote tracking ref. Use exact ref existence,
            NOT a grep: `git branch -r | grep "origin/<branch>"` also matches
            origin/<branch>-bar and origin/feat/<branch>, so a branch would be
            reported as having a remote that does not exist.
              git show-ref --verify --quiet refs/remotes/origin/<branch>
            Exit 0 = the remote ref exists; exit 1 = it does not.
          - Upstream gone flag:
              git for-each-ref --format="%(upstream:track)" refs/heads/<branch>
            "gone" means remote was deleted.

      Step 3 — enumerate worktree directories (GOTCHA 7: some orphaned dirs
        exist that git worktree list does NOT track).

        If {include_worktrees} is not "true", SKIP this step entirely: emit
        "orphaned_worktree_dirs": [] and leave every branch's worktree_* fields
        null. Branch classification still runs; worktrees are simply not swept.
        Do not silently sweep them anyway — a declared knob that does nothing is
        worse than no knob, because the operator believes worktrees are safe.
        a. Tracked worktrees:
             git worktree list --porcelain
           Parse each entry: worktree path, HEAD commit, branch.
        b. Scan for untracked worktree directories under .claude/worktrees/:
             ls -la .claude/worktrees/ 2>/dev/null || true
           For each subdirectory, check whether git worktree list includes it.
           Directories not in that list are orphaned and must be handled as
           plain filesystem directories (git worktree remove will fail on them
           with "is not a working tree").
        c. For each worktree directory (tracked and orphaned), collect:
           - Birth time (macOS): stat -f '%B' <dir>
           - Last modified:      stat -f '%m' <dir>
           - Idle seconds = now_unix - last_modified_unix
           GOTCHA 5 detail: report both birth and last-modified; idle time
           (last-modified) is what determines staleness.

      Step 4 — collect GitHub PR state.
        GOTCHA 6: grep -P is unavailable on macOS BSD grep. Use awk or plain
        grep for any text processing.

        Run:
          gh pr list --state all \
            --json headRefName,state,mergeCommit,number,title \
            --limit 500
        Parse output with awk or python3 -c (no grep -P).
        Build a map: branch_name -> {pr_number, pr_state, merge_commit_sha, title}.
        Branches with no PR entry map to {pr_number: null, ...}.

      Step 5 — for each branch, check worktree dirty status carefully.
        GOTCHA 8: a sparse/broken worktree's git status can reflect the SHARED
        CHECKOUT, not its own content. A worktree may report "9 modified files"
        that are actually the main checkout's files bleeding through.

        For each tracked worktree that has an associated branch:
          (cd <worktree_path> && git status --short 2>&1) | head -20
        For any file reported as modified or untracked:
          test -f <worktree_path>/<reported_file> || echo "FILE_NOT_PRESENT"
        If the file does not physically exist in the worktree directory, the
        dirty status is bleed-through from the main checkout — discard it.
        Only count dirty files that physically exist in the worktree.

      Step 6 — for each branch with uncommitted worktree changes (real ones,
        after bleed-through filtering), diff against main.
        GOTCHA 9: uncommitted WIP is real work that dies with the directory.
          git diff origin/main -- <file> 2>&1 | head -60
        Run this from within the worktree directory to capture its state.
        Store the diff output in the inventory for report to surface separately.

      Step 7 — write {workspace}/outputs/inventory.json:
        {
          "fetched_at": "<ISO-8601 timestamp>",
          "idle_hours_threshold": {idle_hours},
          "protect_list": [<parsed from {protect} param>],
          "branches": [
            {
              "name": "<branch>",
              "reflog_unix": <unix timestamp or null>,
              "has_remote": true|false,
              "upstream_gone": true|false,
              "pr": {
                "number": <int or null>,
                "state": "MERGED|OPEN|CLOSED|null",
                "merge_commit_sha": "<sha or null>",
                "title": "<title or null>"
              },
              "worktree_path": "<path or null>",
              "worktree_tracked": true|false,
              "worktree_birth_unix": <int or null>,
              "worktree_mtime_unix": <int or null>,
              "worktree_idle_seconds": <int or null>,
              "worktree_real_dirty_files": ["<file>", ...],
              "worktree_wip_diff": "<diff text or null>"
            }
          ],
          "orphaned_worktree_dirs": [
            {
              "path": "<absolute path>",
              "birth_unix": <int>,
              "mtime_unix": <int>,
              "idle_seconds": <int>,
              "files": ["<relative file path>", ...]
            }
          ]
        }
    agent:
      role: researcher
      model: sonnet
      tools: [Bash, Read]
      access: read-only
    required: true
    outputs:
      - name: inventory
        mode: generate
        description: >
          Raw inventory JSON written to {workspace}/outputs/inventory.json.
          Contains per-branch reflog ages, PR state, worktree paths, real dirty
          files (bleed-through filtered), WIP diffs, and orphaned worktree dirs.
    writes_to:
      - outputs/inventory.json

  # ===========================================================================
  # G1: Classify branches into deletion-candidate buckets
  # ===========================================================================

  - name: classify-buckets
    kind: execute
    depends_on: [gather-inventory]
    reads_from: [gather-inventory]
    description: >
      Read {workspace}/outputs/inventory.json and apply the correct merged-ness
      signals to bucket each branch. Write a structured JSON with an "items"
      array for fan-out.

      NEVER use git cherry (GOTCHA 1) or git diff ranges (GOTCHA 2) here.

      The ONLY correct merged-ness signals are:

        Signal A — PR record + merge-base:
          For a branch with pr.state == "MERGED" and a non-null merge_commit_sha:
            git merge-base --is-ancestor <merge_commit_sha> origin/main
          Exit 0 = commit IS ancestor of main → confirmed merged.
          Exit 1 = commit IS NOT ancestor of main.

          GOTCHA 3b — rebase-merged PRs: a rebase merge rewrites commits so
          the mergeCommit SHA is not an ancestor of main. Exit 1 does NOT mean
          unmerged. Fall back to Signal B (content comparison), NOT Signal C.
          Signal C counts log commits, which a rebase or squash rewrites by
          definition — it cannot distinguish "rebase-merged" from "never
          merged" and would send every rebase-merged branch to HAS_UNIQUE_WORK.

        Signal B — content in main (for branches with no PR or rebase-merged):
          If the branch has files not in main, check whether those file paths
          exist at origin/main. Options come BEFORE the tree-ish; anything
          after it is a pathspec, so `ls-tree origin/main --name-only <path>`
          asks git for a file literally named "--name-only":
            git ls-tree --name-only origin/main -- <path>

          Do NOT test presence by exit status. ls-tree exits 0 for a path that
          does not exist in the tree, so `&&` on it always succeeds. Presence
          is EMPTY vs NON-EMPTY output:
            if [ -n "$(git ls-tree --name-only origin/main -- "$path")" ]; then
              # path exists in main
            fi
          A cleaner single-path check is cat-file, which does use exit status:
            git cat-file -e "origin/main:$path" 2>/dev/null

          For key files, compare content:
            git show "origin/main:<path>" | diff - <local_path>
          Only flag as "content not in main" if files with meaningful content
          (>10 lines) are absent from main entirely.

        Signal C — commits in branch not in main (for no-PR branches):
          git log origin/main..<branch> --oneline | wc -l
          Returns 0 if all commits reachable from branch are also in main.
          NOTE: This can mis-classify squash-merged branches (GOTCHA 1 variant:
          squash-merged branch log commits are NOT in main even though the work
          is). If this returns >0, do NOT auto-classify as unmerged — route to
          verify-candidates for deeper inspection.

      In-use detection (GOTCHA 4):
        A branch with no PR and content not in main is NOT necessarily orphaned
        — another session may be actively building on it.

        For every non-protected branch, check:
          a. Descendants: does any other local branch build ON TOP of this one?

             The test is NOT bare `merge-base --is-ancestor <branch> <other>`.
             That is true whenever <other> merely contains <branch>'s tip,
             which includes the case where both point at the SAME commit. In
             this repo 44 of 66 branches sat on five shared tips (ephemeral
             `claude -w` branches cut from the same base and never committed
             to), so the bare check flagged 54 of 66 as having a descendant and
             left zero deletion candidates. It reports "everything is in use",
             which reads exactly like a clean repo — the F1 false-clean this
             workflow exists to prevent, reproduced inside the workflow.

             The guard exists to protect work someone BUILT ON. A branch with
             no commits of its own has no such work: it is an empty pointer at
             an old base, and every newer branch trivially "descends" from it
             once main moves past. In this repo 48 of 66 branches were empty
             pointers, which is what produced the 54-of-66 false positive.

             Check all three, in this order — the first is the cheapest and
             eliminates most candidates:
               # 1. Does THIS branch carry work worth protecting?
               own=$(git rev-list --count "origin/main..$branch")
               if [ "$own" -eq 0 ]; then
                 : # No commits of its own -> nothing was built on it.
                   # Do NOT flag HAS_DESCENDANT. The branch may still be
                   # spared by recent activity or by its bucket, but "someone
                   # built on it" is not the reason.
               else
                 for other in $branches; do
                   [ "$other" = "$branch" ] && continue
                   git merge-base --is-ancestor "$branch" "$other" || continue
                   ahead=$(git rev-list --count "$branch..$other")
                   [ "$ahead" -gt 0 ] || continue   # equal tips are not descendants
                   flag HAS_DESCENDANT "$branch" "descendant: $other"
                   break
                 done
               fi

             All three conditions are required. Dropping any one of them
             reproduces the original failure: with only the merge-base test,
             a real run flagged 54 of 66 branches and returned zero deletion
             candidates.
          b. Recent activity. Two independent signals, and a NULL in either
             one means "no evidence", never "recent":

               worktree_idle_seconds is not null AND < {idle_hours} * 3600
               OR
               reflog_unix is not null AND within the last {idle_hours} hours

             A branch with no worktree has worktree_idle_seconds = null.
             Treating that null as satisfying the test marks every
             worktree-less branch as active and spares it forever — a
             false-clean, and the reason a real run produced zero candidates.
             Test each signal for null before comparing it.

             Note reflog recency is the weaker of the two: a fetch, prune, or
             checkout elsewhere touches the ref without anyone working on the
             branch. Prefer the worktree mtime when a worktree exists.
          c. Current worktree branch: is any tracked worktree currently on
             this branch? Do NOT grep the human-formatted list — the branch
             name also appears in worktree PATHS and inside longer branch
             names, so `git worktree list | grep "<branch>"` both false-positives
             (path contains the name) and, worse, can miss the real case while
             matching an unrelated line. A false negative here deletes a branch
             that is checked out and live.

             gather-inventory already resolved this per branch: a branch is
             checked out in a tracked worktree exactly when its
             branches[].worktree_path is non-null and worktree_tracked is true.
             Prefer that field — it was derived from --porcelain, not from the
             human-formatted list. Equivalent direct check if re-deriving:
                 git worktree list --porcelain \
                   | awk '/^branch /{sub("refs/heads/","",$2); print $2}' \
                   | grep -Fxq "<branch>"
             -F -x -q = fixed-string, whole-line, quiet: an exact match.

        A branch with HAS_DESCENDANT OR recent activity OR current worktree
        is classified as IN_USE regardless of PR state.

      Protected branches: any branch in the protect_list from inventory.json,
      plus "main", is classified PROTECTED and skipped entirely.

      Bucket definitions:
        MERGED_PR         — pr.state=MERGED AND Signal A passes (merge-base confirmed)
        REBASE_MERGED     — pr.state=MERGED AND Signal A fails, Signal B confirms content in main
        CONTENT_IN_MAIN   — no PR OR Signal A fails, Signal C=0 (all commits in main)
        ZERO_COMMIT_STALE — no commits beyond origin/main, no PR, idle > threshold
        HAS_UNIQUE_WORK   — Signal C > 0 commits not in main, no MERGED PR; not yet classified
        IN_USE            — HAS_DESCENDANT or recent activity or current worktree
        ORPHANED_DIR      — entries from inventory.json orphaned_worktree_dirs.
                            NOTE: this bucket holds DIRECTORIES, not branches.
                            Its item carries a "dirs" array, not "branches" —
                            see the schema below. Do not force directory entries
                            into branch shape and do not drop the bucket.
        PROTECTED         — in protect_list or is "main"

      Each bucket is an entry in the items array. Branches can only appear in
      ONE bucket (apply in priority order: PROTECTED > IN_USE > MERGED_PR >
      REBASE_MERGED > CONTENT_IN_MAIN > ZERO_COMMIT_STALE > HAS_UNIQUE_WORK).

      Write {workspace}/outputs/buckets.json:
        {
          "classified_at": "<ISO-8601 timestamp>",
          "items": [
            {
              "bucket": "MERGED_PR",
              "branches": [
                {
                  "name": "<branch>",
                  "evidence": "<one-line justification>",
                  "merge_commit_sha": "<sha or null>",
                  "pr_number": <int or null>,
                  "worktree_path": "<path or null>",
                  "wip_diff": "<diff or null>"
                }
              ]
            },
            {
              "bucket": "IN_USE",
              "branches": [...]
            },
            {
              "bucket": "ORPHANED_DIR",
              "dirs": [
                {
                  "path": "<absolute path>",
                  "evidence": "<one-line justification>",
                  "idle_seconds": <int>,
                  "files": ["<relative file path>", ...],
                  "files_absent_from_main": ["<relative file path>", ...]
                }
              ]
            }
          ]
        }

      ORPHANED_DIR is the one bucket whose item uses "dirs" instead of
      "branches", because it describes untracked directories that have no
      branch. Every other bucket uses "branches". Downstream stages must key
      off the bucket name to know which array to read.

      Every branch from inventory.json must appear in exactly one bucket, and
      every entry in orphaned_worktree_dirs must appear in the ORPHANED_DIR
      bucket.

      Emit an item for EVERY bucket name listed above, even when it has no
      members — branch buckets with "branches": [], ORPHANED_DIR with
      "dirs": []. This is not cosmetic. verify-candidates fans out over these
      items and declares one findings-<BUCKET>.json per bucket in writes_to;
      an omitted item means that agent never runs, its findings file is never
      written, and cross-check then reads a missing file. Downstream stages
      cannot distinguish "bucket was empty" from "the stage failed", so the
      empty item is what makes an empty result legible.
      The "items" key at the top level is required — it is the fan_out source
      for verify-candidates.
    agent:
      role: researcher
      model: sonnet
      tools: [Bash, Read, Write]
      access: read-only
    required: true
    outputs:
      - name: buckets
        mode: generate
        description: >
          Classified buckets JSON written to {workspace}/outputs/buckets.json.
          Contains the "items" array used as fan_out source for verify-candidates.
    writes_to:
      - outputs/buckets.json

  # ===========================================================================
  # G2: Verify candidates — parallel fan-out, one agent per bucket
  # ===========================================================================

  - name: verify-candidates
    kind: execute        # NOT kind: validate — that discards stage.description
                         # at runtime, which would strip the {bucket} context and
                         # the output target from every fan-out agent's prompt.
                         # Same reason workflows/shared/code-review-swarm.yaml
                         # uses kind: execute for its validate-concerns fan-out.
    agent:
      role: unit-validator
      model: sonnet  # sonnet required: distinguishing a rebase-merged PR from a
                     # genuinely unmerged branch is judgment, not lookup.
      tools: [Read, Bash, Grep, Write]
      access: read-only
    depends_on: [classify-buckets]
    reads_from: [classify-buckets, gather-inventory]
    fan_out:
      source: classify-buckets
      field: items
      key: bucket
      mode: agent
    description: >
      You are verifying the "{bucket}" bucket from the fan-out.

      Read {workspace}/outputs/buckets.json and select the single item whose
      "bucket" field equals "{bucket}". Verify ONLY that item — another agent
      owns each of the others.

      Which array to read depends on the bucket. Every bucket carries a
      "branches" array EXCEPT ORPHANED_DIR, which carries "dirs" — untracked
      directories that have no branch at all. If {bucket} is ORPHANED_DIR,
      iterate "dirs" and treat each entry's "path" as the subject. Apply
      checks 3 and 4; skip the branch-only checks (1, 2, 5, 6), which have no
      meaning for a directory with no ref. Check 3 (uncommitted WIP) very much
      does apply — an orphaned directory is where stranded work is MOST likely
      to be hiding, since no branch points at it and no commit-based signal can
      see it. For every other bucket, iterate
      "branches". An empty array is valid — emit an empty findings list, never
      an error.

      Your job is adversarial: for each subject, attempt to REFUTE the claim
      "this is safe to delete". You are not confirming the classifier;
      you are trying to break it. Apply every check below and default to
      NOT_SAFE whenever a signal is ambiguous, missing, or contradictory. A
      wrong NOT_SAFE costs one surviving branch; a wrong SAFE destroys work.

      Checks, all of which must pass before a subject may be marked SAFE
      (checks 1, 2, 5 and 6 are branch-only — skip them for ORPHANED_DIR):

        1. Independently re-derive the merged signal. Do not trust the bucket
           label. Re-run the PR record check and merge-base ancestry yourself.

        2. A rebase-merged PR must NOT be marked UNSAFE merely because
           merge-base fails — fall back to content comparison (Signal B).
           Equally, do not mark it SAFE on the PR record alone.

        3. Surface uncommitted WIP regardless of bucket. Uncommitted work dies
           with the directory and is invisible to every commit-based signal.
           If WIP exists, mark NOT_SAFE and include the diff.

           For a branch with a worktree: run git status there, then diff each
           modification against main. Apply GOTCHA 8 — a sparse or broken
           worktree reports the SHARED CHECKOUT's dirty files, so confirm each
           reported file physically exists in that worktree before believing
           it.

           For an ORPHANED_DIR subject there is no branch and git status is
           meaningless — it would report the shared checkout. Instead compare
           the directory's own files against main directly:
             git show origin/main:<relpath> | diff - <dir>/<relpath>
           A file absent from main, or differing from it, is the WIP here.

        4. Orphaned directories: compare their files against main before
           marking deletable. Absent-from-main content means NOT_SAFE.

        5. No branch with a descendant branch may be marked SAFE — deleting it
           breaks the descendant.

        6. No branch currently checked out in any worktree may be marked SAFE.
           Use the exact-match check, never a substring grep.

      Write {workspace}/outputs/findings-{bucket}.json:
        {
          "bucket": "{bucket}",
          "verified_at": "<ISO-8601>",
          "findings": [
            {
              "branch": "<name>",
              "verdict": "SAFE" | "NOT_SAFE",
              "refutation_attempted": "<what you tried in order to break the claim>",
              "evidence": "<commands run and their actual output>",
              "wip_diff": "<diff or null>",
              "blocking_reason": "<why NOT_SAFE, or null>"
            }
          ]
        }

      Every subject in your bucket — each branch, or each dir for
      ORPHANED_DIR — must appear exactly once in findings. Use the branch name
      as "branch" for branch buckets; for ORPHANED_DIR use the directory path
      as "branch" so the findings shape stays uniform for cross-check.
    outputs:
      - name: bucket_findings
        mode: generate
        description: >
          Per-bucket adversarial findings written to
          {workspace}/outputs/findings-{bucket}.json for each bucket processed.
    writes_to:
      - outputs/findings-MERGED_PR.json
      - outputs/findings-REBASE_MERGED.json
      - outputs/findings-CONTENT_IN_MAIN.json
      - outputs/findings-ZERO_COMMIT_STALE.json
      - outputs/findings-HAS_UNIQUE_WORK.json
      - outputs/findings-IN_USE.json
      - outputs/findings-ORPHANED_DIR.json
      - outputs/findings-PROTECTED.json

  # ===========================================================================
  # G3: Cross-check — consistency across all bucket findings
  # ===========================================================================

  - name: cross-check
    kind: validate
    agent:
      role: cross-unit-validator
      model: sonnet
      tools: [Read, Bash, Grep, Write]
      access: read-only
    description: >
      Cross-unit consistency check across all bucket findings. Verify no branch
      appears in two buckets, no deletion candidate has a descendant, no candidate
      is a live worktree's current branch, and protected names are excluded.
      The binding contract is in validation.criteria below.
    depends_on: [verify-candidates]
    reads_from: [verify-candidates, classify-buckets, gather-inventory]
    validation:
      strategy: cross_unit
      criteria:
        - No branch appears in two bucket findings files
        - No deletion candidate has another local branch as a descendant (GOTCHA 4, 10)
        - No deletion candidate is the currently checked-out branch of any live worktree
        - All branches in the protect_list appear in PROTECTED findings, nowhere else
        - The "main" branch is in PROTECTED findings and not in any other bucket
        - Every branch from inventory.json appears in exactly one findings file,
          and every entry in orphaned_worktree_dirs appears in the ORPHANED_DIR
          findings file. ORPHANED_DIR subjects are directory PATHS, not branch
          names — do not report them as missing branches, and do not expect
          them in the branch accounting above
        - Any branch with non-null wip_diff is flagged with WIP_PRESENT regardless of bucket
        - Orphaned directory entries from inventory.json each have a findings entry
      max_revisions: 1
    required: true
    writes_to:
      - validation/cross-check.json

  # ===========================================================================
  # G4: Report — human-readable summary with per-branch evidence
  # ===========================================================================

  - name: report
    kind: execute
    depends_on: [cross-check]
    reads_from: [cross-check, verify-candidates, classify-buckets, gather-inventory]
    description: >
      Produce a human-readable deletion plan with per-branch evidence showing
      why each branch is safe to delete or why it is spared.

      Read all findings files from {workspace}/outputs/findings-*.json and
      {workspace}/validation/cross-check.json.

      Structure the report as:

        ## Branch Hygiene Sweep — Deletion Plan
        Run at: <timestamp>
        dry_run: {dry_run}
        idle_hours threshold: {idle_hours}

        ### Safe to Delete (<N> branches)
        | Branch | Bucket | Evidence | PR | WIP |
        |--------|--------|----------|----|-----|
        | wf-abc123-foo | MERGED_PR | PR #142 merged, merge-base confirmed ancestor | #142 | none |
        | ... | ZERO_COMMIT_STALE | 0 commits beyond main, no PR, idle 72h | — | none |

        Note worktrees: for each deletion candidate that has an associated
        worktree, list the worktree path and note it will also be removed.

        Note orphaned directories: list each orphaned_worktree_dir from the
        inventory separately, with its files and the evidence that all content
        is already in main (or flag if content is not in main).

        ### WIP Present — SPARED (requires manual review)
        List every branch where wip_diff is non-null. Show a summary of the diff.
        These are NEVER deletion candidates even if the branch is merged — the
        uncommitted work was real (GOTCHA 9: in a real run, one such branch
        contained a genuine unshipped bug fix).

        ### In Use — SPARED
        | Branch | Reason | Descendant Branches |
        |--------|--------|---------------------|

        ### Has Unique Work — SPARED (needs PR or manual decision)
        | Branch | Commits Not In Main | Suggestion |
        |--------|---------------------|------------|

        ### Protected — Always Spared
        List the protect_list plus "main".

        ### Summary
        Deletion candidates: N branches, M worktree directories, K orphaned dirs
        Spared (in-use): ...
        Spared (unique work): ...
        Spared (WIP present): ...
        Protected: ...

      Write {workspace}/outputs/deletion-plan.md.
    agent:
      role: doc-writer
      model: sonnet
      tools: [Read, Write]
      access: read-write
    required: true
    outputs:
      - name: deletion_plan
        mode: generate
        description: >
          Human-readable deletion plan written to {workspace}/outputs/deletion-plan.md.
    writes_to:
      - outputs/deletion-plan.md

  # ===========================================================================
  # G5: Human gate — present plan, require approval before any deletion
  # ===========================================================================

  - name: human-gate
    kind: validate
    agent:
      role: reviewer
      model: sonnet
      tools: [Read, Write]
      access: read-only
    description: >
      Present the deletion plan and require explicit approval before anything is
      removed. NOTE: kind:validate prompts are built by dispatch.py's _validate(),
      which never reads stage.description — so every instruction that must reach
      the agent lives in validation.criteria below, phrased as an action rather
      than an assertion. This text is orientation for human readers only.
    human_gate: true
    depends_on: [report]
    reads_from: [report, cross-check]
    validation:
      strategy: deliverable
      criteria:
        - Read {workspace}/outputs/deletion-plan.md and display it to the user in full, including the per-branch evidence column
        - Read {workspace}/validation/cross-check.json and display any finding whose status is FAIL or WARN
        - State the exact counts before asking - how many branches, how many worktrees, how many orphaned directories are proposed for deletion
        - State whether dry_run is true or false, and say plainly that dry_run=true deletes nothing regardless of the answer given
        - List every branch carrying uncommitted WIP separately and prominently, because that work is unrecoverable once deleted
        - Ask for explicit approval and wait - never infer approval from silence or from the absence of an objection
        - Write the verbatim decision and its timestamp to {workspace}/validation/human-gate-decision.md
        - If the user rejects or gives an ambiguous answer, record REJECTED - anything other than clear approval is a rejection
      max_revisions: 0
    required: true
    writes_to:
      - validation/human-gate-decision.md

  # ===========================================================================
  # G6: Execute deletions — always runs; deletes only when dry_run=false
  #     AND the gate approved. There is no `when:` guard on this stage:
  #     it self-checks {dry_run} and writes a sentinel so writes_to is
  #     satisfied on every path.
  # ===========================================================================

  - name: execute-deletions
    kind: execute
    depends_on: [human-gate]
    reads_from: [human-gate, report, classify-buckets]
    description: >
      Delete branches and worktrees that were approved in human-gate.

      If {dry_run} is "true", write a sentinel file and exit without touching
      anything:
        echo "dry_run=true — no deletions performed" > {workspace}/outputs/deletions.md
      This sentinel is always written so writes_to is satisfied on all paths.

      Before anything else, read the gate's decision. Being downstream of
      human-gate in the DAG is sequencing, not consent: the engine runs this
      stage whatever the user answered.

        Read {workspace}/validation/human-gate-decision.md.
        Proceed ONLY if it records an explicit approval.
        If it records REJECTED, is missing, is empty, or is ambiguous, write
          echo "gate not approved — no deletions performed" > {workspace}/outputs/deletions.md
        and exit without deleting anything. Fail closed: a decision file you
        cannot read is a rejection, never an approval.

      If {dry_run} is "false" AND the gate recorded approval, proceed:

      Step 1 — safety patch per branch (always do this first, before any deletion).
        For each approved deletion candidate branch:
          git format-patch origin/main..<branch> \
            --output-directory {workspace}/patches/<branch>/
        This saves all commits that were not yet in main at patch time.
        If the branch has zero commits beyond main, the patches directory will
        be empty — that is expected.
        If git format-patch fails for any branch, SKIP that branch deletion and
        log the error. Never delete without a patch attempt.

      Step 2 — delete tracked worktrees before deleting their branches.
        For each deletion candidate that has worktree_tracked=true:
          git worktree remove --force <worktree_path>
        If this fails, log the error and skip the associated branch deletion.

      Step 3 — delete orphaned worktree directories.
        GOTCHA 7: orphaned directories are NOT tracked by git worktree list.
          git worktree remove <orphaned_dir> will fail with "is not a working tree".

        This is the single most dangerous command in the workflow: a recursive
        delete on a path that came from a filesystem scan rather than from git.
        An empty variable, a relative path, a trailing "..", or a symlinked
        directory turns it into a recursive delete somewhere else entirely.
        Guard every one of those before deleting. For each orphaned_worktree_dir
        in the approved plan:

          # 1. Must be non-empty.
          [ -n "$dir" ] || { echo "SKIP: empty path"; continue; }

          # 2. Resolve symlinks and relative segments to a real absolute path.
          #    Deleting the *link target* is the escape this prevents.
          real=$(realpath "$dir" 2>/dev/null) || { echo "SKIP: unresolvable $dir"; continue; }

          # 3. Must be a real directory, not a symlink to one.
          [ -d "$real" ] && [ ! -L "$dir" ] || { echo "SKIP: not a real dir $dir"; continue; }

          # 4. Must sit strictly INSIDE the worktrees root, after resolution.
          root=$(realpath "$(git rev-parse --show-toplevel)/.claude/worktrees")
          case "$real" in
            "$root"/?*) ;;                      # ok: a child of the root
            *) echo "SKIP: outside $root: $real"; continue ;;
          esac

          # 5. Never the root itself, and never the current directory.
          [ "$real" != "$root" ] || { echo "SKIP: refusing to delete the root"; continue; }
          [ "$real" != "$PWD" ] || { echo "SKIP: refusing to delete cwd"; continue; }

          # 6. Only now, and always with -- to stop option injection.
          rm -rf -- "$real"

        A skip here is never fatal: log it and carry on to the next directory.
        Only run this after verify-candidates confirmed the directory's files
        are present in main (check 4).

      Step 4 — delete local branches.

        Re-check the protect list HERE, immediately before each deletion. It is
        applied during classification and verified afterwards by verify-post,
        but neither helps at the moment that matters: a misclassification or a
        malformed buckets.json would reach this step, and verify-post reports
        the loss only once the branch is already gone. This is the last point
        where refusing still costs nothing.

          protected=$(python3 -c "import json;print('\n'.join(json.load(open('$WS/outputs/inventory.json'))['protect_list']))")
          if printf '%s\n' "$protected" "main" | grep -Fxq "$branch"; then
            echo "REFUSED: $branch is protected"; continue
          fi

        Refuse rather than warn: a protected branch reaching this step means an
        upstream stage is wrong, and deleting it would destroy exactly what the
        operator named as off-limits.

        Do NOT write `git branch -d X || git branch -D X`. That one-liner
        escalates to a force delete on ANY failure of -d — unmerged work,
        a branch checked out elsewhere, a lock file — because the shell cannot
        see the "only when confirmed merged" condition that belongs to it. The
        fallback must be gated in the workflow, not by ||.

        For each approved deletion candidate (after worktrees are handled):

          if git branch -d "$branch" 2>&1; then
            : # safe delete succeeded — git agreed the work is merged
          elif [ "$confirmed_merged" = "true" ]; then
            # -d refused, but Signal A (merge-base) or Signal B (content in
            # main) confirmed this branch. That combination means -d is
            # objecting to a squash or rebase merge it cannot see, which is
            # exactly the case GOTCHA 1 describes.
            git branch -D "$branch" 2>&1
          else
            echo "SKIP: $branch — -d refused and merge was never confirmed"
          fi

        confirmed_merged is true ONLY for branches whose findings entry came
        from the MERGED_PR or REBASE_MERGED bucket with verdict SAFE. A branch
        from any other bucket never reaches the -D path, whatever -d says.

      Step 5 — delete remote tracking refs for branches that had upstream_gone=true.
        For any deleted branch where has_remote was true and upstream_gone was false
        (the remote branch still exists), do NOT delete the remote ref — only local.
        Remote deletion is out of scope for this workflow.

      Write {workspace}/outputs/deletions.md:
        | Branch | Worktree | Patches Saved | Deleted | Error |
        |--------|----------|---------------|---------|-------|
        | wf-abc123-foo | .claude/worktrees/abc123 | 0 patches (zero commits) | YES | — |
        | ... | — | 3 patches | YES | — |
        | bad-branch | — | PATCH_FAILED | SKIPPED | format-patch error |
    agent:
      role: code-writer
      model: sonnet
      tools: [Read, Write, Bash]
      access: read-write
    required: true   # MUST stay required: this stage always writes
                     # outputs/deletions.md (a sentinel even on dry-run), and
                     # reclaim-venvs + verify-post both depend on it. With
                     # required:false a crash mid-deletion would not halt the
                     # DAG, and verify-post would audit an unknown tree
                     # against a missing deletion log.
    outputs:
      - name: deletions
        mode: generate
        description: >
          Deletion log written to {workspace}/outputs/deletions.md.
          Lists every branch processed, patches saved, and outcome.
    writes_to:
      - outputs/deletions.md


  # ===========================================================================
  # G7: Reclaim .venv from kept worktrees — the actual disk win
  # ===========================================================================

  - name: reclaim-venvs
    kind: execute
    depends_on: [execute-deletions]
    reads_from: [execute-deletions, human-gate, classify-buckets, gather-inventory]
    description: >
      Purge regenerable .venv directories from worktrees that are being KEPT.

      Why this stage exists: on this repo a worktree is ~93% .venv (~348M of
      ~350M; source is ~27M). Branch deletion reclaims almost nothing. This
      stage is where the disk actually comes back, and it is far safer than
      deleting a worktree — a .venv is rebuilt on demand with `make venv`,
      while a deleted worktree destroys uncommitted WIP permanently.

      Skip entirely if {reclaim_venvs} is not "true". Write the sentinel and
      exit so writes_to is satisfied on all paths:
        echo "reclaim_venvs=false — no venvs purged" > {workspace}/outputs/venv-reclaim.md

      This stage is gated on the human decision exactly like execute-deletions.
      Read {workspace}/validation/human-gate-decision.md first; if it records
      REJECTED, is missing, empty, or ambiguous, write the sentinel and exit
      without purging anything. A rejected plan means the operator declined the
      whole sweep, not just the branch half.

      This stage honours {dry_run} like every other destructive stage. If
      {dry_run} is "true", enumerate and report what WOULD be reclaimed, then
      exit without deleting anything:
        write the table to {workspace}/outputs/venv-reclaim.md with a
        "DRY RUN — nothing was deleted" heading and a total of what would
        have been freed.

      An earlier revision let this stage delete under dry_run, on the reasoning
      that a .venv is regenerable and therefore not "work". That was wrong for
      one reason that outweighs it: an operator who sets dry_run=true is asking
      for a preview, and a preview that deletes several gigabytes is a broken
      promise regardless of how cheap the bytes are to rebuild. dry_run means
      nothing is removed.

      {reclaim_venvs} remains a separate opt-out, for the case where deletions
      are wanted but venv purging is not.

      Step 1 — enumerate candidate venvs.
        For every worktree still present after execute-deletions:
          find .claude/worktrees -maxdepth 2 -name .venv -type d
        For each, take the PARENT directory's idle time:
          stat -f '%m' <worktree_dir>
        A venv is a candidate when parent idle_hours >= {venv_idle_hours}.

      Note the enumeration is by DIRECTORY, not by branch, so it also covers
        orphaned directories that survived execute-deletions (ORPHANED_DIR
        entries the gate declined, or any dir when {include_worktrees} is
        false). That is intended: a .venv is regenerable regardless of whether
        git tracks the directory holding it. Only the branch-based exclusion
        below is skipped for a dir with no branch — every other exclusion still
        applies.

      Step 2 — exclude anything live. Never purge a venv when its worktree:
          - is the current session's worktree (compare against $PWD), OR
          - is checked out to a PROTECTED or IN_USE branch (read buckets.json).
            A directory with no branch cannot match this check — skip it for
            those and rely on the remaining exclusions, OR
          - has uncommitted changes that include files under .venv (unusual, but
            a hand-edited venv is not regenerable), OR
          - has a running process with a cwd inside it. Check cheaply:
              lsof +D <worktree_dir>/.venv 2>/dev/null | head -1
            If lsof reports anything, SKIP — an active test run or REPL lives
            there and removing it mid-run produces confusing import errors.

      Step 3 — record sizes BEFORE deleting, so the report is truthful:
          du -sk <worktree>/.venv

      Step 4 — purge. Same discipline as execute-deletions Step 3: this is a
        recursive delete on a path assembled from a scan, so prove the target
        is exactly a .venv directory inside the worktrees root before removing
        it.

          [ -n "$wt" ] || { echo "SKIP: empty worktree path"; continue; }
          venv=$(realpath "$wt/.venv" 2>/dev/null) || { echo "SKIP: unresolvable"; continue; }

          # Must be a real directory, not a symlink to one.
          [ -d "$venv" ] && [ ! -L "$wt/.venv" ] || { echo "SKIP: not a real dir"; continue; }

          # Must be named .venv, so a malformed $wt cannot redirect the delete.
          [ "$(basename "$venv")" = ".venv" ] || { echo "SKIP: not a .venv: $venv"; continue; }

          # Must resolve strictly inside the worktrees root.
          root=$(realpath "$(git rev-parse --show-toplevel)/.claude/worktrees")
          case "$venv" in
            "$root"/?*) ;;
            *) echo "SKIP: outside $root: $venv"; continue ;;
          esac

          rm -rf -- "$venv"
        A .venv is not tracked by git, so there is no branch or worktree state
        to update afterwards. Do NOT run git worktree prune here — nothing about
        the worktree registration changed.

      Step 5 — write {workspace}/outputs/venv-reclaim.md with a table of
      worktree, idle hours, size reclaimed, and a total. State plainly that
      each purged worktree needs `make venv` before its next test run.
    agent:
      role: code-writer
      model: sonnet
      tools: [Bash, Read, Write]
      access: read-write
    required: false
    outputs:
      - name: venv_reclaim
        mode: generate
        description: >
          Table of purged venvs with reclaimed size, or a sentinel when the
          stage was skipped.
    writes_to:
      - outputs/venv-reclaim.md

  # ===========================================================================
  # G8: Verify post — confirm protected branches intact, counts match plan
  # ===========================================================================

  - name: verify-post
    kind: validate
    agent:
      role: unit-validator
      model: sonnet
      tools: [Read, Bash, Grep, Write]
      access: read-only
    description: >
      Confirm the post-deletion tree matches the approved plan. The binding
      contract is in validation.criteria below.
    depends_on: [reclaim-venvs]
    reads_from: [reclaim-venvs, execute-deletions, report, gather-inventory]
    validation:
      strategy: deliverable
      criteria:
        - "If dry_run=true, verify the tree is byte-identical - no branch deleted,
          no worktree or orphaned directory removed, and no .venv purged. Confirm
          every .venv present in inventory.json is still present on disk. Only the
          sentinel files and the report may have been written"
        - All protected branches (protect_list + main) still exist in git branch output
        - The main checkout HEAD is unchanged from inventory fetch (no accidental commit)
        - Count of deleted branches matches the approved plan from deletion-plan.md
        - Every branch in the IN_USE bucket still exists
        - Every branch in the HAS_UNIQUE_WORK bucket still exists
        - No patches directory under {workspace}/patches/ is missing for a deleted branch
      max_revisions: 0
    required: true
    writes_to:
      - validation/post-check.json
