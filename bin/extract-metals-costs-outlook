#!/usr/bin/env bash
set -euo pipefail

if [[ -x ".venv/bin/python" ]]; then
  PY=".venv/bin/python"
else
  PY="python3"
fi

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$SCRIPT_DIR/.."
export PYTHONPATH="$REPO_ROOT:${PYTHONPATH:-}"

exec "$PY" -m mail_assistant.utils.outlook_metals_costs "$@"

