from __future__ import annotations

from core.meta_base import AppMeta

META = AppMeta(
    app_id="phone",
    purpose="Home Screen layout planning + manifest helpers",
    display_name="Phone",
    bin_name="./bin/phone",
    example_cmd="./bin/phone layout validate --json",
)
