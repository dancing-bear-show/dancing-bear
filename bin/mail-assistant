#!/usr/bin/env bash
# POSIX shim to run the Mail Assistant without invoking python explicitly.
set -euo pipefail
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "$SCRIPT_DIR/mail_assistant" "$@"

